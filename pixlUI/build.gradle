apply plugin: 'com.android.library'

project.archivesBaseName = 'pixlui'

android {
    compileSdkVersion 31
    buildToolsVersion '28.0.3'

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 31
    }
}

dependencies {
    testImplementation 'junit:junit:4.+'
}
