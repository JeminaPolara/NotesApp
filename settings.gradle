dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
//        mavenCentral()
    }
}

rootProject.name = "Notes"
include ':app',':pixlUI'/*,':simplegallery'*/
